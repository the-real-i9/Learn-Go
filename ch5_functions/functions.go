package main

func main() {
	/* webPage, err := getWebPage(os.Args[1])
	if err != nil {
		fmt.Fprintf(os.Stderr, "getWebPage: %v\n", err)
		os.Exit(1)
	} */

	// findlinks1(webPage)
	// findtags(webPage)
	// countElements(webPage)
	// funcVals()
	// anonymFuncs()
	// variadicFuncs()
	defFuncCalls()
}
