module learn/ch1

go 1.21.3

// this is a module
// all go files in this module (folder) can be compiled to one executable
// a module (folder) like this can also be a package "ch1" (if not "main"), which must be declared in all the go files within this module (folder) as "package ch1"